import { type ContentRouting, contentRouting } from '@libp2p/interface/content-routing';
import { type PeerRouting, peerRouting } from '@libp2p/interface/peer-routing';
import { type IPNSRecord } from 'ipns';
import type { DelegatedRoutingV1HttpApiClient, DelegatedRoutingV1HttpApiClientInit, PeerRecord } from './index.js';
import type { AbortOptions } from '@libp2p/interface';
import type { PeerId } from '@libp2p/interface/peer-id';
import type { CID } from 'multiformats';
export declare class DefaultDelegatedRoutingV1HttpApiClient implements DelegatedRoutingV1HttpApiClient {
    #private;
    private started;
    private readonly httpQueue;
    private readonly shutDownController;
    private readonly clientUrl;
    private readonly timeout;
    private readonly contentRouting;
    private readonly peerRouting;
    /**
     * Create a new DelegatedContentRouting instance
     */
    constructor(url: string | URL, init?: DelegatedRoutingV1HttpApiClientInit);
    get [contentRouting](): ContentRouting;
    get [peerRouting](): PeerRouting;
    isStarted(): boolean;
    start(): void;
    stop(): void;
    getProviders(cid: CID, options?: AbortOptions): AsyncGenerator<PeerRecord, any, unknown>;
    getPeers(peerId: PeerId, options?: AbortOptions | undefined): AsyncGenerator<PeerRecord, any, unknown>;
    getIPNS(peerId: PeerId, options?: AbortOptions): Promise<IPNSRecord>;
    putIPNS(peerId: PeerId, record: IPNSRecord, options?: AbortOptions): Promise<void>;
}
//# sourceMappingURL=client.d.ts.map